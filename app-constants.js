const ResourceNotFound = 'Requested Resource Not Found';

const InvalidID = 'Invalid ID';

const AuthorizationRequired = 'Authorization Required';

const InsufficientPermissions = 'Insufficient Permissions';

const InvalidSyntax = 'Invalid Syntax';

const ConflictingEmail = 'Conflicting Email';

const InternalServerError = 'Internal Server Error';

const IncorrectEmailPassword = 'Incorrect Email or Password';

module.exports = {
  AuthorizationRequired,
  ConflictingEmail,
  InsufficientPermissions,
  InternalServerError,
  InvalidID,
  InvalidSyntax,
  ResourceNotFound,
  IncorrectEmailPassword,
};
